# Matlab Probability

1. `mean`

   ```matlab
   M = mean(A)
   M = mean(A,'all')
   M = mean(A,dim)
   ```

2. `var`

   ```matlab
   V = var(A)
   V = var(A,w)
   V = var(A,w,'all')
   V = var(A,w,dim)
   ```

   
